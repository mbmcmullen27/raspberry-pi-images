---

name: RaspiOs + Docker

on: workflow_dispatch

jobs:
  packer:
    runs-on: ubuntu-latest
    name: Packer Build  

    steps:
      - run: type sponge

      - name: Checkout Repository
        uses: actions/checkout@master  

      - name: Packer Build
        run: |
          cd pi-imager
          ./fetch-image.sh -u templates/docker-host.json
          docker run --rm --privileged -v /dev:/dev -v ${PWD}:/build mkaczanowski/packer-builder-arm build raspios.json
          cd ..
      
      - name: Compress Image
        run: tar -czvf custom-raspios-arm.tar.gz pi-imager/custom-raspios-arm.img

      - name: Create Release Tag
        id: tag 
        run: |
          TAG="custom-$(./pi-imager/fetch-image.sh -v)"
          git tag $TAG 
          git push origin $TAG
          echo ::set-output name=TAG::${TAG}

      - name: Update Release Notes
        run: echo "${{ steps.tag.outputs.TAG }}" >> release.md

      - name: Create Release
        uses: ncipollo/release-action@v1
        id: create_release
        with:
          tag: ${{ steps.tag.outputs.TAG }}
          bodyFile: "release.md"
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Attach Image to Release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ${{ github.workspace }}/custom-raspios-arm.tar.gz
          asset_name: custom-raspios-arm.tar.gz
          asset_content_type: application/gzip